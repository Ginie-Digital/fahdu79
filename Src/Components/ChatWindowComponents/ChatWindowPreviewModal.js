import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ChatWindowPreviewModal = () => {
  return (
    <View>
      <Text>ChatWindowPreviewModal</Text>
    </View>
  )
}

export default ChatWindowPreviewModal

const styles = StyleSheet.create({})